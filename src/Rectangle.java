public class Rectangle {
    float largeur, longeur;
    float perimetre(float n1, float n2){
        return (n1 * 2 + n2 * 2);
    }
    float diagonale(float n1, float n2){
        return(n1 * n1 + n2 * n2);
    }
    float doubler(float n1, float n2){
        return (n1 * 2);
    }
}
